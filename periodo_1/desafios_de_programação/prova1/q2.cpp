#include <stdio.h>
#include <iostream>
#include <vector>
#include <algorithm>

#include <stdio.h>
#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
#include <math.h>
using std::vector;
using std::string;
using std::cout;
using std::cin;
using std::endl;
using std::swap;
using std::pair;
using std::make_pair;

int main(){


    int nCarros, idCarros;

    vector <int> pilhas;

    cin >> nCarros;

    int input[nCarros];
    int output[nCarros];
    for (int i = 0; i < nCarros; i++)
    {
        cin >> idCarros;
        input[nCarros] = i;
    }
    for (int i = 0; i < nCarros; i++)
    {
        cin >> idCarros;
        output[nCarros] = i;
        if(output[nCarros]-input[nCarros]>=1)
        f
    }
    
    



    
}